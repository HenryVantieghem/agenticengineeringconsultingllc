# MCP API Keys Reference

All API keys are stored in `.env` at the project root.
Load them as environment variables — never hardcode.

firecrawl
FIRECRAWL_API_KEY=<see .env>

apify
APIFY_API_TOKEN=<see .env>

github
GITHUB_TOKEN=<see .env>

openai
OPENAI_API_KEY=<see .env>

context7
CONTEXT7_API_KEY=<see .env>
